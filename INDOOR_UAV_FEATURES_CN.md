# 室内无人机飞控自定义功能说明

本文档记录在 DJI Mobile SDK V5 示例工程基础上新增的室内无人机飞控相关功能。本次改动以不影响 DJI 官方示例功能为原则，将自定义能力集中在室内飞控入口和相关页面中。

## 1. 室内无人机飞控入口

工程新增了独立的室内无人机飞控入口，用于在大疆 Android 遥控器上快速进入自定义飞控工作台。

进入室内飞控后，默认打开室内直播飞控页面，而不是普通菜单页。页面面向真实室内飞行场景，优先展示无人机实时画面。

## 2. 全屏无人机直播飞控页面

新增室内专用直播页面，用于显示无人机相机实时画面。

主要特点：

- 使用 DJI MSDK `MediaDataCenter.cameraStreamManager` 接入无人机图传。
- 使用全屏 `SurfaceView` 显示直播画面。
- 页面底部保留三个功能按钮，减少对画面的遮挡。
- 隐藏调试性质的相机源文字，例如 `Camera: LEFT_OR_MAIN`。
- 该页面不复用或修改 DJI 官方 `LiveFragment`，避免影响官方示例功能。

底部三个功能入口为：

- 建模与航点采集
- ArUco 引导降落
- UDP 控制

## 3. 无人机电池电量显示

室内直播飞控页面左下角新增无人机电池电量显示。

显示内容包括：

- 自绘电池图标
- 电池百分比文本

电量显示通过 DJI MSDK `BatteryKey.KeyChargeRemainingInPercent` 获取和监听飞机电池剩余百分比。

颜色规则：

- 电量大于 40%：白色
- 电量 21% 到 40%：黄色
- 电量小于等于 20%：红色

该显示放在底部按钮栏上方的左下角，采用半透明背景，尽量减少对直播画面的遮挡。

## 4. 建模与航点采集页面

原功能名称“建模与巡检航点采集”调整为“建模与航点采集”。

该页面用于连接机载计算机，并向机载端发送航点采集请求。

当前交互调整为：

- 页面进入后自动读取已保存的机载计算机 IP 和 TCP 端口。
- 自动尝试与配置的机载计算机建立通信。
- 隐藏“建立通信”和“断开通信”按钮。
- 右侧只显示简化后的通信状态。
- 保留 IP 和端口输入框，便于现场修改。
- IP 和端口修改后会保存到 App 本地，下次重启 App 后仍然保留。

通信状态显示简化为：

- 正在连接...
- 通信正常
- 通信失败

连接成功后，“测试通信”和“采集当前航点”按钮可用；未连接或连接失败时按钮不可用。

## 5. 机载计算机通信配置持久化

建模与航点采集页面使用 Android `SharedPreferences` 保存机载计算机 IP 和 TCP 端口。

默认配置为：

- IP：`192.168.1.20`
- 端口：`9001`

用户修改后，配置会保存在本地，不会因为 App 重启而恢复默认值。

## 6. ArUco 引导降落相关能力

工程保留并集成了 ArUco 引导降落入口。该功能基于无人机相机画面和 OpenCV ArUco 检测，用于识别标记并辅助降落对准。

相关能力包括：

- ArUco 标记检测
- 识别结果叠加显示
- 云台朝下控制入口
- 自动对齐入口
- 自动降落入口
- 停止检测入口

## 7. UDP 控制相关能力

工程保留并集成了 UDP 控制入口，用于接收外部设备发送的控制数据，并在用户启动飞控后转发到虚拟摇杆控制链路。

UDP 页面会自动监听指定端口，并显示接收到的控制数据和飞控状态。

## 8. 与 DJI 官方示例的隔离原则

本次自定义功能主要集中在室内无人机飞控相关页面和类中，尽量避免修改 DJI 官方示例页面。

自定义新增内容包括：

- 室内飞控 Activity
- 室内飞控导航图
- 室内直播飞控 Fragment
- 室内直播页面布局
- 室内电池图标控件
- 建模与航点采集页面和通信逻辑

这样可以保留 DJI 官方示例的参考价值，同时在独立入口中继续迭代室内无人机飞控业务功能。

## 9. 构建验证

已使用 Android Studio 自带 JBR 进行 Debug 构建验证：

```bash
JAVA_HOME="D:/RuanJian/Android Studio/jbr" PATH="D:/RuanJian/Android Studio/jbr/bin:$PATH" ./gradlew :sample:assembleDebug
```

构建结果：

```text
BUILD SUCCESSFUL
```

构建日志中存在 DJI SDK 和官方示例原有资源、API 废弃警告，不影响本次新增功能编译通过。
